"""slackwebapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from slackwebapi.api_helper import APIHelper
from slackwebapi.configuration import Server
from slackwebapi.controllers.base_controller import (
    BaseController,
)
from slackwebapi.exceptions.pins_add_error_schema_exception import (
    PinsAddErrorSchemaException,
)
from slackwebapi.exceptions.pins_list_error_schema_exception import (
    PinsListErrorSchemaException,
)
from slackwebapi.exceptions.pins_remove_error_schema_exception import (
    PinsRemoveErrorSchemaException,
)
from slackwebapi.http.http_method_enum import (
    HttpMethodEnum,
)
from slackwebapi.models.pins_add_schema import (
    PinsAddSchema,
)
from slackwebapi.models.pins_remove_schema import (
    PinsRemoveSchema,
)


class PinsController(BaseController):
    """A Controller to access Endpoints in the slackwebapi API."""

    def __init__(self, config):
        """Initialize PinsController object."""
        super(PinsController, self).__init__(config)

    def pins_add(self,
                 token,
                 channel,
                 timestamp=None):
        """Perform a POST request to /pins.add.

        Pins an item to a channel.

        Args:
            token (str): Authentication token. Requires scope: `pins:write`
            channel (str): Channel to pin the item in.
            timestamp (str, optional): Timestamp of the message to pin.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Typical success response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/pins.add")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("token")
                .value(token)
                .is_required(True))
            .form_param(Parameter()
                .key("channel")
                .value(channel)
                .is_required(True))
            .form_param(Parameter()
                .key("timestamp")
                .value(timestamp))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("slackAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(PinsAddSchema.from_dictionary)
            .is_api_response(True)
            .local_error("default",
                "Typical error response",
                PinsAddErrorSchemaException),
        ).execute()

    def pins_list(self,
                  token,
                  channel):
        """Perform a GET request to /pins.list.

        Lists items pinned to a channel.

        Args:
            token (str): Authentication token. Requires scope: `pins:read`
            channel (str): Channel to get pinned items for.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Typical success response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/pins.list")
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                .key("token")
                .value(token)
                .is_required(True))
            .query_param(Parameter()
                .key("channel")
                .value(channel)
                .is_required(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("slackAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.dynamic_deserialize)
            .is_api_response(True)
            .local_error("default",
                "Typical error response",
                PinsListErrorSchemaException),
        ).execute()

    def pins_remove(self,
                    token,
                    channel,
                    timestamp=None):
        """Perform a POST request to /pins.remove.

        Un-pins an item from a channel.

        Args:
            token (str): Authentication token. Requires scope: `pins:write`
            channel (str): Channel where the item is pinned to.
            timestamp (str, optional): Timestamp of the message to un-pin.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Typical success response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/pins.remove")
            .http_method(HttpMethodEnum.POST)
            .header_param(Parameter()
                .key("token")
                .value(token)
                .is_required(True))
            .form_param(Parameter()
                .key("channel")
                .value(channel)
                .is_required(True))
            .form_param(Parameter()
                .key("timestamp")
                .value(timestamp))
            .header_param(Parameter()
                            .key("content-type")
                            .value("application/x-www-form-urlencoded"))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("slackAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(PinsRemoveSchema.from_dictionary)
            .is_api_response(True)
            .local_error("default",
                "Typical error response",
                PinsRemoveErrorSchemaException),
        ).execute()
